[project]
name = "quadstar-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "joosm4n", email = "kotoole0703@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy>=2.0.52",
    "tifffile>=2026.8.23",
    "zstandard>=0.25.0",
]

[project.scripts]
quadstar-data = "quadstar_data:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
